<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="midnight_dice" parent="Theme.AppCompat.NoActionBar" >
        <item name="colorAccent">@color/StarryBlue</item>
        <item name="colorPrimary">@color/SteelBlack</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="android:colorBackground">@color/SteelBlack</item>
        <item name="android:colorForeground">@color/WhiteSmoke</item>
        <item name="android:textColorPrimaryInverse">@color/black</item>
        <item name="android:textColorSecondary">@color/WhiteSmoke</item>
        <item name="android:textColorPrimary">@color/AliceBlue</item>
        <item name="android:textColorSecondaryInverse">@color/SteelBlack</item>
        <item name="android:windowBackground">@color/SteelBlack</item>
    </style>

</resources>
